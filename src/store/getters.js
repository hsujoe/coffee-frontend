export const product = (state) => {
  return state.product
}

export const products = (state) => {
  return state.products
}

export const store = (state) => {
  return state.store
}

export const stores = (state) => {
  return state.stores
}

export const selectItem = (state) => {
  return state.selectItem
}

export const saveProduct = (state) => {
  return state.saveProduct
}

export const simpleReccords = (state) => {
  return state.simpleReccords
}
